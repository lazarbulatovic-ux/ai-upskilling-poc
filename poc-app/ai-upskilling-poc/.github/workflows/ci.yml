name: CI

on:
  push:
    branches: [main, master, '**']
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore SalesChatbot.sln

      - name: Build
        run: dotnet build SalesChatbot.sln -c Release --no-restore

      - name: Test
        run: dotnet test SalesChatbot.sln -c Release --no-build --verbosity normal

      - name: Format check
        run: dotnet format SalesChatbot.sln --verify-no-changes
